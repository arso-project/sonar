const Schema = require('./lib/schema')
const Store = require('./lib/store')
const { Record } = require('./lib/records')
const Type = require('./lib/type')

module.exports = { Schema, Store, Record, Type }
